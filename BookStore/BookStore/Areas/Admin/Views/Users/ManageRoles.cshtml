@model UserRoles

@{
    ViewData["Title"] = "ManageRoles";
}

<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Manage Roles</h4>
                <h4 class="card-title text-success">User Id: @Model.UserId</h4>
                <form method="post">
                    <input asp-for="UserId" type="hidden" />
                    <div class="table-responsive">
                        <table class="table text-center" id="order-listing" cellspacing="0" style="width:100%">
                            <thead>
                                <tr>
                                    <th>Role</th>
                                    <th>Status</th>
                                </tr>
                            </thead>
                            <tbody>
                                @for(int i = 0; i < Model.Roles.Count; i++)
                                {
                                    <tr>
                                        <input asp-for="Roles[i].Value" type="hidden" />
                                        <td>@Model.Roles[i].Value</td>
                                        <td>
                                            <div class="form-check">
                                              <input class="form-check-input" type="checkbox" asp-for="Roles[i].Status" id="flexCheckDefault" />
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <button asp-action="Save" type="submit" class="btn btn-primary btn-lg mt-5">Save</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

